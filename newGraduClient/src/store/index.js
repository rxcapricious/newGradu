import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const initState = {};

const mutations = {};

const actions = {};

export default new Vuex.Store({
    state: initState,
    mutations,
    actions,
    strict: false
});